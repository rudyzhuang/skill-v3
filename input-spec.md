# Skill V3 需求说明（给人看的草稿）

本文档描述「这一版 skill 想做成什么样」，方便讨论和改字。不涉及具体技术怎么实现；实现时可以对照上一版仓库里的约定、脚本和提示语，但**不要求**把技术细节写进本文。

---

## 一、和上一版的关系

- **上一版在本地的位置**：`/Users/guodongzhuang/.cursor/skills/`
- **上一版在 GitHub 上的仓库**：https://github.com/rudyzhuang/skills.git

这一版在**整体思路上延续上一版**：仍然是按阶段推进、有清晰的输入输出、用脚本和提示语把各阶段串起来。需要改哪里、补哪里，可以以旧仓库为参考，再在本仓库里定稿。

---

## 二、这一版想多解决什么问题

上一版已经能跑通一条链路，这一版希望**再往前走一步**：

- skill 的**输入**要更清楚：每阶段「进来的是什么」要说死、说全，减少含糊和漏项。
- skill 的**输出**要更可靠：每阶段「交出去的是什么」要可核对、可对齐下一阶段的期待，减少对不上号的情况。
- 为此，要给**每一个阶段**单独补上「约束说明」：这一阶段允许什么、不允许什么、必须满足什么才算过关——先写在给人看的文档里，后面再落到机器可执行的规则里。

一句话：**更稳、更对、更完整**，主要靠「每个阶段的约束写清楚」来托底。

---

## 总体阅读：阶段顺序与 skill 分工

读文档时可以先看这一节，再往下翻各阶段的占位说明：这里把**整条链上阶段叫什么、谁先谁后**，以及**一个大 skill 里会覆盖哪几段**，用一张「路线图」固定下来，和上一版习惯大体一致，只标出**新增**和**改名**的地方。

### 阶段链（与上一版对齐，并标出变化）

整体顺序延续大家已经熟悉的那条路，在此基础上：

- **新增**：在「契约」之后增加 **design-review**（设计侧再过一遍、把设计和契约对齐）；在流水线末尾增加 **report**（汇总、交代结果给人看）。
- **改名说明**：**merge-push** 这一段干的还是「合并并推到远端」这类事，和上一版里大家常说的 **ai-git2** 那一段职责相当，只是文档里统一用这个叫法。

**推荐固定的先后关系**（从左到右就是默认推进顺序）：

`prd` → `prd-review` → `design` → `contract` → `design-review` → `codegen` → `typecheck` → `test` → `code-review` → `merge-push` → `build` → `deploy` → `smoke` → `report`

**关于 deploy 和 smoke**：链上是「先部署、再冒烟」；实际用时还会区分 **开发环境**和**正式发布环境**，这在下面的 **skill 分工**里拆开写，避免和「整条链的大顺序」打架。

### skill 与阶段：一对多

一个 **skill** 可以串多段 **stage**（阶段），是**一对多**：人既可以点某个大 skill 一次跑完它负责的那几段，也可以继续用「单阶段手工」的方式拆着跑——只要遵守第三节里「不同跑法结果要对齐」的约定。

| skill 名称（本版） | 覆盖的阶段 |
| --- | --- |
| **ai-prd3** | prd，prd-review |
| **ai-design3** | design，contract，design-review |
| **ai-code3** | codegen，typecheck，test，code-review，merge-push，build |
| **ai-publish-dev3** | deploy（**dev** 环境），smoke |
| **ai-publish-release3** | deploy（**release** 环境），smoke |

**report** 未写进上表：可以交给「整条流水线的编排」或单独再拆一个 skill，由你在实现定稿时二选一或组合用；本文先把**阶段链**里必须有 **report** 这一点钉死。

---

## 三、各阶段说明（占位，待你补全）

### 执行方式上的总体约定（先对齐预期）

- **手工跑某一阶段，和自动一路跑下去，结果要一致**  
  每个阶段对应的 skill，可以单独由人触发、一步一步做；也可以交给「负责整条流水线」的编排 skill，按顺序自动调用各阶段。两种用法只是**谁按按钮、谁排队**不同，在「输入相同、规则相同」的前提下，**各阶段该产出什么、最终效果应对齐**，不能因为换了一种跑法就变一套结果。

- **同一阶段重复跑：要稳，但要改已有结果时，手工跑要先问人**  
  在输入不变的前提下，同一阶段多跑几次，**输出应当一致、可预期**（不搞「同题不同卷」）。  
  若本阶段**已经有产出**，再跑会**覆盖**掉这些产出，则属于「可能动到已有成果」的情况：在**只跑单个阶段、由人手工触发**时，必须先**提示清楚会覆盖什么**，并得到**用户明确同意**后再继续。  
  **说明**：这一条只约束「单阶段、手工执行」的体验；**自动编排**那条线里怎么避免误覆盖，由编排侧自己的规则单写（本条不套用在自动编排上）。

- **自动编排：开跑前先验输入；途中遇到「这阶段已经做完」就跳过去**  
  编排从**第一个阶段**动身之前，先把后面各阶段要用到的**输入条件**都过一遍：哪一项不满足事先写好的要求，就**立刻停**，并给出**人看得懂的说明**（缺什么、哪里不对），不要糊里糊涂往下跑。  
  执行到某一阶段时，若发现**该阶段的输出已经存在**，且状态表明**这一阶段已经算完成**，则**不要重复干一遍**，而是明确提示「这一阶段已完成」，然后**直接进入下一阶段**。

---

阶段名与顺序见上一节「**总体阅读**」；下面共 **14** 个小节，与阶段链从左到右一一对应。每一节只写「这一阶段打算管什么事」和「约束打算往哪方面写」的**占位**，侧重业务语言，实现细节（脚本、文件名等）不在这里展开。

### 阶段 1：prd（占位）

**打算管的事**：（待写，例如：把业务想法整理成可执行、可拆分的书面需求。）

**约束打算写清楚的方向**：（待写，例如：谁必须提供什么信息、产出里哪些块不能空、和下一阶段交接时不能含糊的地方。）

---

### 阶段 2：prd-review（占位）

**打算管的事**：（待写。）

**约束打算写清楚的方向**：（待写。）

---

### 阶段 3：design（占位）

**打算管的事**：（待写。）

**约束打算写清楚的方向**：（待写。）

---

### 阶段 4：contract（占位）

**打算管的事**：（待写。）

**约束打算写清楚的方向**：（待写。）

---

### 阶段 5：design-review（占位）

**打算管的事**：（待写。）

**约束打算写清楚的方向**：（待写。）

---

### 阶段 6：codegen（占位）

**打算管的事**：（待写。）

**约束打算写清楚的方向**：（待写。）

---

### 阶段 7：typecheck（占位）

**打算管的事**：（待写。）

**约束打算写清楚的方向**：（待写。）

---

### 阶段 8：test（占位）

**打算管的事**：（待写。）

**约束打算写清楚的方向**：（待写。）

---

### 阶段 9：code-review（占位）

**打算管的事**：（待写。）

**约束打算写清楚的方向**：（待写。）

---

### 阶段 10：merge-push（占位，职责与上一版 ai-git2 相当）

**打算管的事**：（待写。）

**约束打算写清楚的方向**：（待写。）

---

### 阶段 11：build（占位）

**打算管的事**：（待写。）

**约束打算写清楚的方向**：（待写。）

---

### 阶段 12：deploy（占位）

**打算管的事**：（待写。）

**约束打算写清楚的方向**：（待写。）

---

### 阶段 13：smoke（占位）

**打算管的事**：（待写。）

**约束打算写清楚的方向**：（待写。）

---

### 阶段 14：report（占位）

**打算管的事**：（待写。）

**约束打算写清楚的方向**：（待写。）

---

## 四、文档怎么用

- 先把第三部分的「打算管的事」和「约束方向」用**业务语言**填实（共 14 个阶段）；填顺了，再让实现侧去对齐输入输出和检查项。
- 若某一阶段和上一版差异很大，在对应小节里用一句话标出来即可，不必在本文写实现方案。

（文末可随讨论追加：名词表、角色分工、和外部系统的边界等，需要再加。）
